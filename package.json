{
  "name": "lego-controller",
  "version": "1.0.0",
  "description": "Bluetooth LEGO controllet",
  "main": "index.js",
  "scripts": {
    "start": "node ./dist/test-gpio3.js",
    "clean": "rm -rf ./dist",
    "build": "yotta-build-app",
    "compile": "yotta-compile-app",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "git push",
    "deploy": "ssh -t rasp2.yottabrick.com '/bin/bash -l -c \"cd ~/stash/lego-train-controller && git pull && npm run build\"'"
  },
  "author": "Ben Rogers <ben@bdr.org>",
  "dependencies": {
    "@highflying/dev-tools": "^1.1.2",
    "@highflying/node-poweredup": "^1.1.9",
    "@types/noble": "0.0.38",
    "@types/pigpio": "^1.2.0",
    "keypress": "^0.2.1",
    "micro": "^9.3.3",
    "microrouter": "^3.1.3",
    "pigpio": "^1.2.1",
    "rpi-gpio": "^2.1.2"
  },
  "devDependencies": {
    "@types/micro": "^7.3.3",
    "@types/microrouter": "^3.1.0"
  }
}
